package movietube.movietube.test

import android.content.Context


class Enam constructor(private val context: Context) {
/*  fun test()
  {
      val dialog = Dialog(context)
dialog.setCanceledOnTouchOutside(false)

dialog.getWindow().setFlags(WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE, WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE)

dialog.getWindow().addFlags(WindowManager.LayoutParams.FLAG_ALT_FOCUSABLE_IM or WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON)

dialog.getWindow().getDecorView().setSystemUiVisibility(
    View.SYSTEM_UI_FLAG_LAYOUT_FULLSCREEN
    or View.SYSTEM_UI_FLAG_LAYOUT_STABLE
    or View.SYSTEM_UI_FLAG_HIDE_NAVIGATION
    or View.SYSTEM_UI_FLAG_FULLSCREEN
    or View.SYSTEM_UI_FLAG_IMMERSIVE_STICKY
    or View.SYSTEM_UI_FLAG_LAYOUT_HIDE_NAVIGATION
)

dialog.setContentView(R.layout.dialog_custom)
dialog.setTitle("Immersive Mode Dialog")

dialog.findViewById(R.id.dialogBtnYes).setOnClickListener(object: View.OnClickListener() {
 fun onClick(v:View) {
dialog.dismiss()
}
})

dialog.findViewById(R.id.dialogBtnNo).setOnClickListener(object: View.OnClickListener() {
 fun onClick(v:View) {
dialog.dismiss()
}
})

dialog.show()

dialog.getWindow().clearFlags(WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE)
  }*/
}
